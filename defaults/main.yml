nsd_user: "{{ __nsd_user }}"
nsd_group: "{{ __nsd_group }}"
nsd_db_dir: "{{ __nsd_db_dir }}"
nsd_run_dir: "{{ __nsd_run_dir }}"
nsd_state_dir: "{{ __nsd_state_dir }}"
nsd_service: nsd
nsd_conf_dir: "{{ __nsd_conf_dir }}"
nsd_conf: "{{ nsd_conf_dir }}/nsd.conf"
nsd_config_server: []
nsd_config_remote_control: []
nsd_flags: ""
nsd_remote_setup: false
# master and slave
nsd_zones: {}
nsd_include_role_x509_certificate: no
